{{- if and .Values.global.networkRemediation.enabled .Values.frontend.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "hub.fullname" . }}-frontend
  labels:
    {{- include "hub.labels" . | nindent 4 }}
    app.kubernetes.io/component: frontend
spec:
  replicas: {{ .Values.frontend.replicaCount }}
  selector:
    matchLabels:
      {{- include "hub.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: frontend
  template:
    metadata:
      labels:
        {{- include "hub.labels" . | nindent 8 }}
        app.kubernetes.io/component: frontend
    spec:
      {{- if .Values.global.frontendAuth.enabled }}
      serviceAccountName: {{ include "hub.fullname" . }}-frontend
      {{- end }}
      containers:
        - name: frontend
          image: "{{ .Values.image.registry }}/{{ .Values.image.frontend }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - name: http
              containerPort: 8080
              protocol: TCP
          {{- if .Values.global.frontendAuth.enabled }}
          env:
            # Bind nginx to loopback only, so it's reachable exclusively from
            # the oauth-proxy sidecar sharing this pod's network namespace --
            # otherwise any in-cluster client could hit this pod's IP on 8080
            # directly and skip OAuth entirely. See nginx.conf.template.
            - name: NGINX_LISTEN_ADDRESS
              value: "127.0.0.1"
          {{- end }}
          {{- if .Values.global.frontendAuth.enabled }}
          livenessProbe:
            {{- include "hub.nginxLoopbackProbe" . | nindent 12 }}
          readinessProbe:
            {{- include "hub.nginxLoopbackProbe" . | nindent 12 }}
          {{- else }}
          livenessProbe:
            {{- toYaml .Values.frontend.livenessProbe | nindent 12 }}
          readinessProbe:
            {{- toYaml .Values.frontend.readinessProbe | nindent 12 }}
          {{- end }}
          resources:
            {{- toYaml .Values.frontend.resources | nindent 12 }}
        {{- if .Values.global.frontendAuth.enabled }}
        {{- include "hub.oauthProxyContainer" (dict "component" "frontend" "context" $) | nindent 8 }}
        {{- end }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "hub.fullname" . }}-frontend
  labels:
    {{- include "hub.labels" . | nindent 4 }}
    app.kubernetes.io/component: frontend
spec:
  type: {{ .Values.frontend.service.type }}
  ports:
    - port: {{ .Values.frontend.service.port }}
      {{- if .Values.global.frontendAuth.enabled }}
      targetPort: 8888
      {{- else }}
      targetPort: {{ .Values.frontend.service.targetPort }}
      {{- end }}
      protocol: TCP
      name: http
  selector:
    {{- include "hub.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: frontend
{{- if .Values.global.frontendAuth.enabled }}
---
{{ include "hub.oauthProxyServiceAccount" (dict "component" "frontend" "context" $) }}
{{- end }}
{{- if .Values.global.routes.enabled }}
---
apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: {{ include "hub.fullname" . }}-frontend
  labels:
    {{- include "hub.labels" . | nindent 4 }}
    app.kubernetes.io/component: frontend
spec:
  to:
    kind: Service
    name: {{ include "hub.fullname" . }}-frontend
  port:
    targetPort: http
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
{{- end }}
{{- end }}
